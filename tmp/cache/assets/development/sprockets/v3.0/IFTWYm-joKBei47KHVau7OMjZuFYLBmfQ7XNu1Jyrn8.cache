I"‚(function() {
  var i, len, m, rev;

  rev = JSON.parse(gon.restaurants);

  window.onload = function() {
    var i, len, r, results;
    results = [];
    for (i = 0, len = rev.length; i < len; i++) {
      r = rev[i];
      results.push(rate(r));
    }
    return results;
  };

  window.markers = [];

  google.maps.event.addDomListener(window, 'load', function() {
    var i, len, r, results;
    initmap();
    results = [];
    for (i = 0, len = rev.length; i < len; i++) {
      r = rev[i];
      results.push(markers.push(new google.maps.Marker({
        position: new google.maps.LatLng(r.x, r.y),
        map: map
      })));
    }
    return results;
  });

  for (i = 0, len = markers.length; i < len; i++) {
    m = markers[i];
    m.addListener('click', function() {}, new google.maps.InfoWindow({
      content: "<h2>" + r.address + "</h2>"
    }).open(map, m));
  }

}).call(this);
:ET