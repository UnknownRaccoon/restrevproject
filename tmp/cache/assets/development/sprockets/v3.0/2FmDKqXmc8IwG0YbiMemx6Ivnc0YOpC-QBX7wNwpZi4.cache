I"p(function() {
  var markers, rev;

  rev = JSON.parse(gon.restaurants);

  markers = [];

  window.onload = function() {
    var i, len, r, results;
    results = [];
    for (i = 0, len = rev.length; i < len; i++) {
      r = rev[i];
      results.push(rate(r));
    }
    return results;
  };

  google.maps.event.addDomListener(window, 'load', function() {
    var i, len, marker, r, results;
    initmap();
    results = [];
    for (i = 0, len = rev.length; i < len; i++) {
      r = rev[i];
      marker = new google.maps.Marker({
        position: new google.maps.LatLng(r.x, r.y),
        map: map
      });
      markers.push(marker);
      results.push(marker.addListener('click', function() {
        return new google.maps.InfoWindow({
          content: "<h2>" + r.name + "</h2>"
        }).open(map, this);
      }));
    }
    return results;
  });

}).call(this);
:ET