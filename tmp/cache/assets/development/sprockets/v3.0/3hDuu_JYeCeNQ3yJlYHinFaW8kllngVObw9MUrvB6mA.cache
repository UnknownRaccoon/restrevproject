I"(function() {
  var rev;

  rev = JSON.parse(gon.restaurants);

  window.onload = function() {
    var j, len, r, results;
    results = [];
    for (j = 0, len = rev.length; j < len; j++) {
      r = rev[j];
      results.push(rate(r));
    }
    return results;
  };

  google.maps.event.addDomListener(window, 'load', function() {
    var i, id, infoW, j, len, marker, markers, r, results, text;
    initmap();
    infoW = new google.maps.InfoWindow({
      content: "Test"
    });
    markers = [];
    i = 0;
    results = [];
    for (j = 0, len = rev.length; j < len; j++) {
      r = rev[j];
      alert(r.name);
      text = r.name;
      marker = new google.maps.Marker({
        position: new google.maps.LatLng(r.x, r.y),
        map: map
      }, id = r.id);
      markers.push(marker);
      results.push(markers[markers.length - 1].addListener('click', function() {
        infoW.setContent(rev[i].text);
        i++;
        return infoW.open(map, this);
      }));
    }
    return results;
  });

}).call(this);
:ET