I"Î(function() {
  window.onload = function() {
    return rate(gon.restaurant);
  };

  window.onload = function() {
    var i, len, r, results;
    results = [];
    for (i = 0, len = rest.length; i < len; i++) {
      r = rest[i];
      results.push(rate(r));
    }
    return results;
  };

  google.maps.event.addDomListener(window, 'load', function() {
    var map, mapCanvas, marker, placeMarker;
    mapCanvas = document.getElementById('map');
    map = new google.maps.Map(document.getElementById('map'), {
      center: new google.maps.LatLng(46.4711427, 30.7396057),
      zoom: 14,
      mapTypeId: google.maps.MapTypeId.ROADMAP
    });
    marker = new google.maps.Marker({
      map: map
    });
    navigator.geolocation.getCurrentPosition(function(position) {
      map.setCenter({
        lat: position.coords.latitude,
        lng: position.coords.longitude
      });
      return map.setZoom(16);
    }, function() {});
    return google.maps.event.addListener(map, 'click', function(event) {
      return makrer.setPosition(event.latLng);
    }, placeMarker = function(location) {
      return marker = new google.maps.Marker({
        position: location,
        map: map
      });
    });
  });

}).call(this);
:ET