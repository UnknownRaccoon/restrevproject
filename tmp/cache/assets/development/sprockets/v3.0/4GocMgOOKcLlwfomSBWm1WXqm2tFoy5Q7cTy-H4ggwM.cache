I"!(function() {
  var rev;

  rev = JSON.parse(gon.restaurants);

  window.onload = function() {
    var k, len, r, results;
    results = [];
    for (k = 0, len = rev.length; k < len; k++) {
      r = rev[k];
      results.push(rate(r.id));
    }
    return results;
  };

  google.maps.event.addDomListener(window, 'load', function() {
    var i, infoW, k, len, marker, r, results, text;
    initmap();
    infoW = new google.maps.InfoWindow;
    window.markers = [];
    window.names = [];
    for (k = 0, len = rev.length; k < len; k++) {
      r = rev[k];
      text = r.name;
      marker = new google.maps.Marker({
        position: new google.maps.LatLng(r.x, r.y),
        map: map,
        label: r.name
      });
      names.push(r.name + '<br />' + r.comment);
      markers.push(marker);
    }
    i = 0;
    results = [];
    while (i < names.length) {
      markers[i].addListener('click', function() {
        var j, results1;
        j = 0;
        results1 = [];
        while (j < names.length) {
          if (this.getPosition() === markers[j].getPosition()) {
            infoW.setContent(names[j]);
          }
          infoW.open(map, this);
          results1.push(j++);
        }
        return results1;
      });
      results.push(i++);
    }
    return results;
  });

}).call(this);
:ET