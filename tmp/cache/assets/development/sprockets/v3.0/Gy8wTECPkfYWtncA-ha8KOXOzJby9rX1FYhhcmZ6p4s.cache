I"ó(function() {
  var rev;

  rev = JSON.parse(gon.restaurants);

  window.onload = function() {
    var j, len, r, results;
    results = [];
    for (j = 0, len = rev.length; j < len; j++) {
      r = rev[j];
      results.push(rate(r));
    }
    return results;
  };

  google.maps.event.addDomListener(window, 'load', function() {
    var i, infoW, j, len, marker, name, r, results, text;
    initmap();
    infoW = new google.maps.InfoWindow({
      content: "Test"
    });
    window.markers = new Array();
    for (j = 0, len = rev.length; j < len; j++) {
      r = rev[j];
      text = r.name;
      marker = new google.maps.Marker({
        position: new google.maps.LatLng(r.x, r.y),
        map: map
      });
      markers[r.name] = marker;
    }
    i = 0;
    results = [];
    for (name in markers) {
      results.push(markers[name].addListener('click', function() {
        infoW.setContent(name);
        return infoW.open(map, this);
      }));
    }
    return results;
  });

}).call(this);
:ET