I"Y(function() {
  var rev;

  rev = JSON.parse(gon.restaurants);

  window.onload = function() {
    var i, len, r, results;
    results = [];
    for (i = 0, len = rev.length; i < len; i++) {
      r = rev[i];
      results.push(rate(r));
    }
    return results;
  };

  google.maps.event.addDomListener(window, 'load', function() {
    var i, infoW, len, marker, r, results;
    initmap();
    infoW = new google.maps.InfoWindow({
      content: "Test"
    });
    results = [];
    for (i = 0, len = rev.length; i < len; i++) {
      r = rev[i];
      marker = new google.maps.Marker({
        position: new google.maps.LatLng(r.x, r.y),
        map: map
      });
      results.push(marker.addListener('click', function() {
        infoW.setContent(r.id);
        return infoW.open(this, map);
      }));
    }
    return results;
  });

}).call(this);
:ET